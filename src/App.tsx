import * as React from 'react';
import Note from '@components/note';

export default () => {
     return <div id='root'>
         <Note title="Note #1" body="This is the body of the first note"/>
     </div>
}